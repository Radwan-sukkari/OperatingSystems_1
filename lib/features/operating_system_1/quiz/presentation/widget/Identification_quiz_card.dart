import 'package:flutter/material.dart';

class IdentificationQuizCard extends StatelessWidget {
  const IdentificationQuizCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
